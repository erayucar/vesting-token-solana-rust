[toolchain]

[features]
resolution = true
skip-lint = false

[programs.Devnet]
vesting_token_2024 = "program id"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
